{
  "name": "save-cli",
  "bin": "bin/app.js",
  "version": "1.0.0",
  "description": "simple command line tool letting user registered encrypt password into a local MongoDb database made out of node.js with less dependecies as possible",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "wax",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^3.0.8",
    "mongodb": "^3.5.3"
  }
}